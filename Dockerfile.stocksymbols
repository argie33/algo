FROM python:3.10-slim

# Debug container startup
RUN echo "🚀 DOCKERFILE BUILDING - Stock symbols loader"

WORKDIR /app

COPY requirements-loadstocksymbols.txt ./
RUN pip install --no-cache-dir -r requirements-loadstocksymbols.txt

COPY loadstocksymbols.py ./

# Debug before starting
RUN echo "✅ Container built successfully" && ls -la

CMD echo "🎯 CONTAINER STARTING..." && python -u loadstocksymbols.py
