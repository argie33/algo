# Local development environment variables
# This allows the app to run in fallback mode without errors

# Database configuration (mock - not a real database)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=stocks_dev
DB_USER=postgres
DB_PASSWORD=password
DB_SSL=false

# AWS Configuration for local development
AWS_REGION=us-east-1
WEBAPP_AWS_REGION=us-east-1

# Node environment
NODE_ENV=development