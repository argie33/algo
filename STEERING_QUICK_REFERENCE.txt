================================================================================
STEERING AUDIT - QUICK REFERENCE
2026-08-01 | All Systems: 2071 tests pass | But: 58 issues remain
================================================================================

CRITICAL BLOCKERS (Must Fix Before Any Trading) - 13 Issues
────────────────────────────────────────────────────────────

🔴 EXIT ENGINE CATASTROPHIC FAILURES (6):
1. Live trades NEVER close - status column mismatch (SELECT fills, UPDATE open) ← DO FIRST
2. Hard stop-loss bypassed by min-hold gate - position gaps 20% with zero exit
3. NULL exit prices corrupt P&L - delisted positions close with no pricing
4. Transaction abort cascades - one query fails = entire batch loses exits
5. Position lock TOCTOU race - position modified between read and lock
6. Distribution check raises stop BELOW current price - guaranteed whipsaw

⚠️  PHASE & DATABASE FAILURES (7):
7. Phase 3 nested DatabaseContext still closing caller connections
8. Phase 1 stock symbols empty check only logs, doesn't halt
9. Phase 6 exit engine min-hold gate bypasses hard stops (redundant with #2)
10. Phase 1 grace period: 30-min error (4:00-4:59 treated as 4:00-4:30)
11. Phase 6 paper mode validation inverted (skips in paper, should enforce)
12. Phase 5 regime field not validated (allows invalid values)
13. Database advisory locks no timeout - orchestrator hangs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

HIGH-PRIORITY (Fix Before Test Mode) - 24 Issues
──────────────────────────────────────────────

DASHBOARD FAILURES (4):
- User isolation missing cognito_sub in some queries (privacy violation)
- API cache not invalidated on stale data
- Health panel shows "0 exits" (field name mismatch)
- Phase 5 display empty (missing key mapping)

PHASE LOGIC (8):
- Phase 7 anomaly threshold 200 vs median 300-1000 (massive blind spot)
- Phase 7 liquidity timeout can exceed orchestrator timeout (600s vs 300s)
- Phase 8 'auto' mode grouped with paper checks (causes skipped validation)
- Concentration checks fail silently (oversized positions not forced out)
- Failsafe retry result structure not validated
- Exit trade errors caught, position left open

DATABASE & LOADERS (12):
- Watermark zero-record logic advances on weekend (prevents retry)
- Status manager error_count resets on partial success
- Position queries don't use FOR UPDATE consistently
- Savepoint rollback assumes not aborted (doesn't check)
- Price NULL validation still possible
- Risk calculation accepts negative risk/inverted stops
- SEC API timeout retries missing
- Batch size history resets on long runs
- Upsert ON CONFLICT missing for retries

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHAT WORKS ✅
──────────

- Transaction safety (savepoints, rollbacks phases 1-9)
- Error classification (transient vs permanent)
- Circuit breaker logic (market, broker account)
- Signal quality scoring
- Entry position sizing (constraints, leverage)
- Test suite (2071/2097 passing)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TIMELINE TO PRODUCTION READY
────────────────────────────

PHASE 1: Critical Blockers (Mon-Tue) - 10 hours
  → 8 critical issues in exit engine, Phase 1/3/6, database
  → After: Run full test suite + orchestrator test

PHASE 2: High-Priority Flows (Wed-Thu) - 8 hours
  → 24 high issues in dashboard, phases, loaders, database
  → After: Ready for test mode (paper trading)

PHASE 3: Medium-Priority (Fri) - 5 hours
  → 10 medium issues (post-deployment acceptable)

PHASE 4: Validation (3 days)
  → 3-day paper mode run
  → User isolation test (2 users)
  → Position closure test (verify fix #1 works)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

RECENT FIXES (Already Applied) ✅
─────────────────────────────────

Commit 0da9e99cb: Phase 3 nested database context (CRITICAL #6)
Commit 3909bdb03: Dashboard user isolation query binding (CRITICAL #3 from old audit)
Commit c0a336101: Watermark advancement non-trading days (CRITICAL #4 from old audit)
Commit 9b2a4320d: Phase 1-9 constraint validation (5 medium)
Commit 8a4754765: Cascade prevention, signals, monitoring (5 medium)
Commit a047d5c52: Rollbacks, consistency, timeouts (5 medium)

Still Broken: Exit engine transaction/status logic, Phase 6/7/8 validation gaps,
             database lock issues, dashboard field mismatches

================================================================================
