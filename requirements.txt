psycopg2-binary==2.9.9
python-dotenv==1.2.2
yfinance==1.4.1
requests==2.33.0
pandas==2.1.4
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.5.2
shap==0.46.0
boto3==1.34.0
Flask==3.1.3
Flask-CORS==6.0.0
Werkzeug==3.1.6
urllib3==2.7.0
rich==13.9.4
pydantic==2.8.2
lxml==6.1.0
openpyxl==3.1.0
playwright==1.48.0
# websockets: algo/execution/trade_update_listener.py's raw Alpaca trade_updates stream
# consumer (2026-09-06 real-money-readiness architecture rebuild). Originally implemented
# via the alpaca-trade-api SDK's Stream class, but that SDK hard-pins websockets<11, which
# directly conflicts with yfinance==1.4.1's own websockets>=13.0 floor above - the two could
# never coexist in this requirements.txt (pip ResolutionImpossible). Rewritten to speak
# Alpaca's trading-stream protocol directly over this package instead, consistent with
# every other Alpaca call in this codebase already using raw `requests` rather than an SDK.
# Floor matches yfinance's own >=13.0 requirement, no new upper bound introduced.
websockets>=13.0
