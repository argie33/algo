psycopg2-binary==2.9.9
python-dotenv==1.2.2
yfinance==1.4.1
requests==2.33.0
pandas==2.1.4
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.5.2
shap==0.46.0
boto3==1.34.0
Flask==3.1.3
Flask-CORS==6.0.0
Werkzeug==3.1.6
urllib3==2.7.0
rich==13.9.4
pydantic==2.8.2
lxml==6.1.0
openpyxl==3.1.0
playwright==1.48.0
# alpaca-trade-api: ONLY for algo/execution/trade_update_listener.py's Stream client
# (real-time trade_updates websocket, 2026-09-06 real-money-readiness architecture rebuild).
# Every other Alpaca call in this codebase uses raw `requests` against the REST API
# directly (order_manager.py, position_order_management.py, etc.) - do not use this SDK
# for anything else; that would create two different Alpaca client conventions to keep in
# sync. Version pinned to match the one already used in lambda/requirements.txt.
alpaca-trade-api==3.2.0
