"""Helper base classes for data loaders.

This package contains reusable base classes and utilities for building data loaders.
These are NOT standalone loaders (they don't have main() entry points).

Modules:
- sec_financials_base: Base class for loaders reading SEC XBRL financial data
- sec_edgar_base: Base class for loaders fetching from SEC EDGAR API
"""
