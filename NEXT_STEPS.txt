================================================================================
                         NEXT STEPS — GET TO 100%
================================================================================

CURRENT STATUS:
  Local:  ✅ 100% working (8.1M prices, 216K signals, 282 tests passing)
  AWS:    ⏳ Ready to deploy (code committed, GitHub Actions configured)

TIME TO FULL DEPLOYMENT: 15 minutes
================================================================================

WHAT TO DO NOW:

1. GET CREDENTIALS (5 min)
   - Alpaca paper keys: https://app.alpaca.markets/settings/api-keys
   - FRED API key: fred.stlouisfed.org → settings
   - AWS Console: RDS → get database endpoint

2. CREATE AWS SECRETS (5 min) — Copy-paste these commands:

   aws secretsmanager create-secret --name algo/alpaca \
     --secret-string '{"api_key":"YOUR_ALPACA_KEY","api_secret":"YOUR_ALPACA_SECRET"}' \
     --region us-east-1

   aws secretsmanager create-secret --name algo/fred \
     --secret-string '{"api_key":"YOUR_FRED_KEY"}' \
     --region us-east-1

   aws secretsmanager create-secret --name algo/database \
     --secret-string '{"host":"YOUR_RDS_ENDPOINT","user":"stocks","password":"YOUR_DB_PASSWORD","port":5432,"database":"stocks"}' \
     --region us-east-1

3. DEPLOY (1 command)
   git push origin main

   GitHub Actions will:
   ✓ Run all tests
   ✓ Check security
   ✓ Deploy Terraform (Lambda, RDS, API Gateway, etc.)
   ✓ Set up EventBridge scheduling
   ✓ Deploy Lambda functions with correct env vars

   Monitor at: https://github.com/argie33/algo/actions

4. VERIFY (1 command)
   aws lambda invoke --function-name stocks-algo-prod \
     --payload '{"source":"schedule"}' response.json --region us-east-1

   Should return StatusCode: 200 with execution results

THAT'S IT. System will be 100% live and ready for paper trading.

If you get stuck, see CLAUDE.md (deployment section).

================================================================================
