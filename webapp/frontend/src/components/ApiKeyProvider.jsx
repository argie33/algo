/**
 * ApiKeyProvider Component
 * Simple provider component for API key management
 * Note: Functionality moved to Settings page, this is a compatibility wrapper for tests
 */


const ApiKeyProvider = ({ children }) => {
  return children;
};

export { ApiKeyProvider };
export default ApiKeyProvider;