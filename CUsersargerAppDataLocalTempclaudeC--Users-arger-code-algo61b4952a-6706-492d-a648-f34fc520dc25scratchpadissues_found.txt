=== REAL PROBLEMS FOUND ===

1. INDUSTRIES ENDPOINT (/api/industries) - BROKEN (500 ERROR)
   File: lambda/api/routes/industries.py:273
   Issue: Query selects avg_trailing_pe, avg_pb_ratio but code tries to get "avg_forward_pe"
   Impact: Endpoint fails validation and returns 500
   Fix: Remove line 273 or add avg_forward_pe to query

2. PERF_ANALYTICS FIELDS - ALL NULL
   File: lambda/api/routes/algo_handlers/metrics.py
   Issue: Performance analytics endpoint returns all None values
   Fields affected: sharpe252, sortino, calmar, wr50, avg_w_r, avg_l_r, expectancy, maxdd
   Impact: Dashboard can't show performance metrics
   Root: Need to investigate why _get_performance_analytics returns all-None

3. MISSING TEST COVERAGE  
   Need to verify: These endpoints have tests that would have caught these bugs

4. DATA FRESHNESS
   Dashboard diagnostic shows 0 stale data, but perf_anl has no metrics
   Likely: Data isn't being calculated/populated, not a loader freshness issue
