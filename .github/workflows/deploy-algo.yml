# DEPRECATED — replaced by deploy-all-infrastructure.yml (Terraform-only)
name: deploy-algo (deprecated)
on:
  workflow_dispatch:
  workflow_call:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use deploy-all-infrastructure.yml instead."
