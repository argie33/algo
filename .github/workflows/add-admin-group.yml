name: Add User to Admin Group
on:
  workflow_dispatch:
    inputs:
      username:
        description: 'Username or email to add to admin group'
        required: true
        default: 'admin@stocks.local'

jobs:
  add_admin:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/algo-svc-github-actions-dev
          aws-region: us-east-1

      - name: Resolve Cognito pool ID
        id: cognito_pool
        run: |
          POOL_ID=$(aws cognito-idp list-user-pools --max-results 60 --region us-east-1 --query "UserPools[?Name=='algo-pool-dev'].Id" --output text)
          if [ -z "$POOL_ID" ]; then
            echo "ERROR: Could not find Cognito user pool 'algo-pool-dev'"
            exit 1
          fi
          echo "pool_id=$POOL_ID" >> $GITHUB_OUTPUT

      - name: Create admin group if it doesn't exist
        run: |
          POOL_ID="${{ steps.cognito_pool.outputs.pool_id }}"

          # Try to create the group (will fail if exists, that's ok)
          aws cognito-idp create-group \
            --group-name admin \
            --user-pool-id "$POOL_ID" \
            --description "Administrator group" \
            --region us-east-1 2>/dev/null || echo "Admin group already exists"

      - name: Add user to admin group
        run: |
          POOL_ID="${{ steps.cognito_pool.outputs.pool_id }}"
          USERNAME="${{ github.event.inputs.username }}"

          echo "Adding $USERNAME to admin group..."
          aws cognito-idp admin-add-user-to-group \
            --user-pool-id "$POOL_ID" \
            --username "$USERNAME" \
            --group-name admin \
            --region us-east-1

          echo "✅ User added to admin group"

          # Verify
          echo ""
          echo "Verifying membership..."
          aws cognito-idp admin-list-groups-for-user \
            --user-pool-id "$POOL_ID" \
            --username "$USERNAME" \
            --region us-east-1 | jq '.Groups[] | {GroupName, Description}'
