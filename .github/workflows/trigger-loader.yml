name: Trigger Data Loader

# Manually trigger any ECS data loader by name.
# Useful when a scheduled loader fails and needs a manual retry.

on:
  workflow_dispatch:
    inputs:
      loader_name:
        description: 'Loader name (e.g. company_profile, positioning_metrics, analyst_sentiment)'
        required: true
        type: string
      wait_for_completion:
        description: 'Wait for the task to finish before completing'
        required: false
        default: 'true'
        type: boolean

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  trigger-loader:
    name: Trigger ${{ inputs.loader_name }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332  # v4.1.7

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a  # v4
        with:
          role-to-assume: ${{ secrets.GITHUB_ACTIONS_ROLE_ARN || format('arn:aws:iam::{0}:role/{1}', secrets.AWS_ACCOUNT_ID, secrets.GITHUB_ACTIONS_ROLE_NAME || 'algo-svc-github-actions-dev') }}
          aws-region: us-east-1

      - name: Get network config
        id: network
        run: |
          VPC_ID=$(aws ec2 describe-vpcs --filters "Name=tag:Name,Values=algo*" \
            --region us-east-1 --query "Vpcs[0].VpcId" --output text)
          # Prefer public subnets: ECS tasks need internet access to reach Secrets Manager/ECR.
          # Public subnets with assignPublicIp=ENABLED work without NAT gateway.
          SUBNET=$(aws ec2 describe-subnets \
            --filters "Name=vpc-id,Values=$VPC_ID" "Name=map-public-ip-on-launch,Values=true" \
            --region us-east-1 --query "Subnets[0].SubnetId" --output text)
          if [ -z "$SUBNET" ] || [ "$SUBNET" = "None" ]; then
            SUBNET=$(aws ec2 describe-subnets --filters "Name=vpc-id,Values=$VPC_ID" \
              --region us-east-1 --query "Subnets[0].SubnetId" --output text)
          fi
          SG=$(aws ec2 describe-security-groups \
            --filters "Name=vpc-id,Values=$VPC_ID" "Name=group-name,Values=algo-ecs-tasks-sg" \
            --region us-east-1 --query "SecurityGroups[0].GroupId" --output text)
          echo "subnet=$SUBNET" >> $GITHUB_OUTPUT
          echo "security_group=$SG" >> $GITHUB_OUTPUT

      - name: Resolve task definition
        id: taskdef
        run: |
          TASK_DEF=$(aws ecs list-task-definitions --region us-east-1 \
            --family-prefix "algo-${{ inputs.loader_name }}-loader" \
            --sort DESC --query "taskDefinitionArns[0]" --output text)
          if [ -z "$TASK_DEF" ] || [ "$TASK_DEF" = "None" ]; then
            echo "ERROR: Task definition algo-${{ inputs.loader_name }}-loader not found."
            exit 1
          fi
          echo "task_def=$TASK_DEF" >> $GITHUB_OUTPUT
          echo "Using task definition: $TASK_DEF"

      - name: Run ECS task
        id: run_task
        run: |
          TASK_ARN=$(aws ecs run-task \
            --cluster algo-cluster \
            --capacity-provider-strategy "capacityProvider=FARGATE_SPOT,weight=100,base=0" \
            --task-definition "${{ steps.taskdef.outputs.task_def }}" \
            --network-configuration "awsvpcConfiguration={subnets=[${{ steps.network.outputs.subnet }}],securityGroups=[${{ steps.network.outputs.security_group }}],assignPublicIp=ENABLED}" \
            --region us-east-1 \
            --query "tasks[0].taskArn" --output text)
          echo "task_arn=$TASK_ARN" >> $GITHUB_OUTPUT
          echo "Launched: $TASK_ARN"

      - name: Wait for completion
        if: inputs.wait_for_completion
        timeout-minutes: 120
        run: |
          echo "Waiting for ${{ inputs.loader_name }} to finish (up to 2h)..."
          TASK_ARN="${{ steps.run_task.outputs.task_arn }}"
          MAX_WAIT=7200
          ELAPSED=0
          while [ $ELAPSED -lt $MAX_WAIT ]; do
            STATUS=$(aws ecs describe-tasks \
              --cluster algo-cluster \
              --tasks "$TASK_ARN" \
              --region us-east-1 \
              --query "tasks[0].lastStatus" --output text)
            echo "[$ELAPSED s] Status: $STATUS"
            if [ "$STATUS" = "STOPPED" ]; then
              break
            fi
            sleep 30
            ELAPSED=$((ELAPSED + 30))
          done

          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster algo-cluster \
            --tasks "$TASK_ARN" \
            --region us-east-1 \
            --query "tasks[0].containers[0].exitCode" --output text)
          echo "Exit code: $EXIT_CODE"
          [ "$EXIT_CODE" = "0" ] && echo "${{ inputs.loader_name }} succeeded!" || { echo "${{ inputs.loader_name }} failed with exit code $EXIT_CODE"; exit 1; }
