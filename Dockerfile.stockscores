# Use shared base image with dependencies pre-installed
ARG ECR_REGISTRY
FROM ${ECR_REGISTRY}:base-loader-latest

# Copy only the Python script (dependencies already in base)
COPY loadstockscores.py .
ENTRYPOINT ["python", "loadstockscores.py"]

# REBUILD TRIGGER: 2026-01-26 - Switched to base-loader image for faster builds
# Removed redundant requirements file - uses shared base image dependencies
# Improves deployment speed in AWS ECS by ~5 minutes per build
