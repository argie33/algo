# Lambda orchestrator dependencies
# These will be installed into the algo-orchestrator-layer for use by the Lambda function
# Note: pandas, numpy excluded (not used by orchestrator, causes size bloat)

psycopg2-binary>=3.1.0
boto3>=1.26.0
requests>=2.28.0
python-dateutil>=2.8.2
pytz>=2022.7
pyjwt>=2.6.0
cryptography>=39.0.0
