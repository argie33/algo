# Lambda orchestrator dependencies
# psycopg2 is in separate Lambda Layer (algo-psycopg2-layer-dev)
# numpy/scipy: excluded to keep layer under 70MB AWS direct-upload limit.
# algo_var.py (VaR) handles ImportError gracefully — non-blocking for trading.

boto3>=1.34.0
requests>=2.32.0
python-dateutil>=2.8.2
pytz>=2022.7
pyjwt>=2.8.0
cryptography>=42.0.0
