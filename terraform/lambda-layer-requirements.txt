# Shared-deps Lambda layer — numpy only (scipy is 40+ MB and pushes base64-encoded
# request over Lambda's 70 MB API limit; scipy imports all fail-open already).
# Raw zip must be under ~50 MB so base64-encoded upload stays under 70167211 bytes.
#
# scipy features (weight optimization, IC, attribution) continue to fail-open
# gracefully when scipy is missing — Phase 7 log: "scipy not available".

numpy>=1.24.0
