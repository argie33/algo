# Shared-deps Lambda layer: ONLY packages not already available in Lambda runtime
# or other layers. Lambda runtime already provides boto3, botocore, urllib3.
# The psycopg2 layer provides psycopg2. The API layer provides requests.
#
# Keep this list minimal — numpy+scipy alone are ~60 MB and near the 67 MB direct-upload limit.

# Scientific computing (required for Phase 7 portfolio optimization, VaR, IC)
numpy>=1.24.0
scipy>=1.10.0
