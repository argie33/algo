FROM python:3.10-slim

# Match working stocksymbols Dockerfile pattern exactly
WORKDIR /app

# Copy requirements and install dependencies (minimal approach)
COPY requirements-loadinfo.txt ./
RUN pip install --no-cache-dir -r requirements-loadinfo.txt

# Copy application code
COPY loadinfo.py ./

CMD ["python", "-u", "loadinfo.py"]
